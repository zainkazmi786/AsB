// Department code format: DEPT-{NUMBER} e.g. DEPT-001
export const DEFAULT_DEPARTMENT_CODE_FORMAT = 'DEPT-{NUMBER}';

// Department status options
export const DEPARTMENT_STATUS = {
  ACTIVE: true,
  INACTIVE: false,
};
